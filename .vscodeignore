.vscode/**
.claude/**
.serena/**
.gitignore
.esbuild.ts
.mcp.json
node_modules/**
src/**
test/**
assets/**
docs/**
records/**
tsconfig.json
pnpm-lock.yaml
pnpm-workspace.yaml
vitest.config.ts
eslint.config.js
**/tsconfig.json
**/*.ts
**/*.map
**/.eslintrc.json
**/*.spec.ts
AGENTS.md
CLAUDE.md
TODO.md
**/claude-code-jetbrains-plugin/**
scripts/**
README_CN.md
# An old copy of the Claude Code CLI (2.0.76) nothing loads: sessions run the
# SDK's native binaries (resources/native-binaries/), and the only file Forge used
# from it, ripgrep, now lives in resources/ripgrep/.
resources/claude-code/**
# A dev build's single binary (the build machine's). The VSIX carries the
# release targets' binaries in resources/native-binaries/<target>/ instead.
resources/native-binary/**
.forge-cache/**
# Build leftovers that are not part of a release.
*.vsix
.harness/**
.stylelintrc.json
