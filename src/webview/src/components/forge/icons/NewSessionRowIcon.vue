<template>
  <!--
    The official sessions manager's "New session" glyph: `e51`, which is
    `forwardRef(EM0)`, the 20px solid plus. Copied from the bundle by hand,
    path for path, because `extract-icons.mjs` does not follow this component's
    `p1(...)` createElement wrapper. Sized by the caller (16px in the row).
  -->
  <svg
    viewBox="0 0 20 20"
    fill="currentColor"
    xmlns="http://www.w3.org/2000/svg"
    aria-hidden="true"
    data-slot="icon"
  >
    <path d="M10.75 4.75a.75.75 0 0 0-1.5 0v4.5h-4.5a.75.75 0 0 0 0 1.5h4.5v4.5a.75.75 0 0 0 1.5 0v-4.5h4.5a.75.75 0 0 0 0-1.5h-4.5v-4.5Z" />
  </svg>
</template>
